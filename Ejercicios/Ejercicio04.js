// Ejercicio 4: Ciclos y Alcance de Variables

// 1. Usando `var`
for (var x=0;x<4;x++){
     //Imprime en consola el valor de i
    console.log(x);
  }
console.log(x);
  //Imprime en consola el valor de i

  
  // 2. Usando `let`
  for (let y=0;y<4;y++) {
   //Imprime en consola el valor de i
    console.log(y);
  }
  //Imprime en consola el valor de i
  console.log(y);
  //3. Usando `const`
  //for (const a=0;a>5;a++) {
       //Imprime en consola el valor de i
    //console.log(a)
  //}
  //console.log(a)  